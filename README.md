# Smart Food Constructor

Веб-приложение для умного подбора рецептов на основе имеющихся у пользователя продуктов. Система интегрируется с API Spoonacular для получения данных о рецептах и предоставляет интеллектуальную систему подбора рецептов по ингредиентам.

## Технологический стек

### Backend
- **Django** — веб-фреймворк Python

### Frontend
- **HTML5** — разметка
- **Tailwind CSS** — utility-first CSS фреймворк

### Внешние сервисы
- **Spoonacular API** — API для получения данных о рецептах

## Требования

- Python 3.14+
- Spoonacular API ключ

## 🔧 Установка и запуск

### Локальная разработка

1. **Клонируйте репозиторий:**
```bash
git clone https://github.com/ecl1pseeee-boop/recept_projects
cd recepts
```

2. **Создайте виртуальное окружение:**
```bash
python -m venv .venv
source .venv/bin/activate  # Для Windows: .venv\Scripts\activate
```

3. **Установите зависимости:**
```bash
pip install -r requirements.txt
```

4. **Создайте файл `.env` в корне проекта:**
```env
SECRET_KEY=change-me-to-a-long-random-string
DEBUG=false

# Домены / контейнеры
APP_CONTAINER_NAME=django-app
DB_CONTAINER_NAME=django-db
NGINX_CONTAINER_NAME=nginx-proxy

# Django
DJANGO_SETTINGS_MODULE=DjangoUlearn.settings

DB_NAME=your_database_name
DB_USER=your_database_user
DB_PASSWORD=your_database_password
DB_ROOT_PASSWORD=your_root_password
DB_HOST=db
DB_PORT=3306

# API
API_KEY=your-api-key
SPOONACULAR_URL="https://api.spoonacular.com"

```

5. **Примените миграции:**
```bash
python manage.py migrate
```

6. **Создайте суперпользователя:**
```bash
python manage.py createsuperuser
```

7. **Запустите сервер для локальной разработки:**
```bash
python manage.py runserver
```

Приложение будет доступно по адресу: `http://127.0.0.1:8000`

## Конфигурация


##  Интеграция с Spoonacular API

Проект интегрирован с Spoonacular API для получения данных о рецептах. Используемые endpoints:

- Поиск рецептов
- Детали рецепта
- Рецепты по ингредиентам

##  Frontend особенности

- **HTMX**: Динамическая загрузка контента без перезагрузки страницы, отправка форм через AJAX, бесконечная прокрутка
- **Alpine.js**: Клиентская интерактивность, выпадающие меню, модальные окна, валидация форм
- **Tailwind CSS**: Адаптивный дизайн (mobile-first), консистентная стилизация

## Разработка

### Создание миграций
```bash
python manage.py makemigrations
python manage.py migrate
```

### Сбор статических файлов
```bash
python manage.py collectstatic
```

### Доступ к админ-панели
После создания суперпользователя админ-панель доступна по адресу: `http://127.0.0.1:8000/admin`

**Примечание**: Для работы приложения необходим действующий API ключ Spoonacular. Получить ключ можно на сайте [spoonacular.com](https://spoonacular.com/food-api)
